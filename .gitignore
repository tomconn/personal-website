# macOS Finder specific files
# These store custom folder attributes (like icon positions) and should not be versioned.
.DS_Store

# While the rule above covers this, explicitly listing it can sometimes improve clarity
# for specific build/ignore tooling, though it's generally redundant for Git itself.
# functions/.DS_Store
